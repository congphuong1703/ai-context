/** pytest rules */
export const RULE_PYTEST = `## pytest
- Fixtures for shared setup; parametrize for multiple inputs
- One assertion per test when possible; use \`pytest.raises\` for exceptions
- Place tests in \`tests/\`; mirror source layout`;
