/**
 * GIT & COLLABORATION — universal foundation.
 * Applies to all languages and stacks.
 */

export const RULES_GIT_COLLABORATION = `## 📌 GIT & COLLABORATION
- Atomic commits: one logical change per commit; message in imperative mood.
- Branch naming: type/short-desc (e.g. feat/auth, fix/validation, chore/refactor, hotfix/security).
- Never force-push to shared branches; rebase only on private branches.
- Resolve conflicts by preserving intent of both sides when possible.
- Prefer small, reviewable PRs; link tickets and document non-obvious decisions.`;
