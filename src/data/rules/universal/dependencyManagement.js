/**
 * DEPENDENCY MANAGEMENT — universal foundation.
 * Applies to all languages and stacks.
 */

export const RULES_DEPENDENCY_MANAGEMENT = `## 📦 DEPENDENCY MANAGEMENT
- Pin exact versions in lockfiles; do not commit without updating lockfile.
- Audit dependencies regularly for known vulnerabilities.
- Prefer minimal dependency set; avoid adding deps for one-off use.
- Prefer well-maintained, widely used libraries over niche alternatives.
- Document why non-obvious or heavy dependencies are required.`;
