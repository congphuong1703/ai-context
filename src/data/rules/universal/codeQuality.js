/**
 * CODE QUALITY — universal foundation.
 * Applies to all languages and stacks.
 */

export const RULES_CODE_QUALITY = `## 📐 CODE QUALITY
- Descriptive names: verbs for functions, nouns for variables/types.
- No magic numbers or strings — named constants only.
- All public APIs documented: JSDoc / Javadoc / docstring + example where helpful.
- Functions: max 60 lines, max 6 params (use DTOs/options objects beyond that).
- Prefer composition over inheritance.
- Remove dead code immediately — never comment out and leave.`;
